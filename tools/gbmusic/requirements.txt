# Shared by both paths (see README.md):
demucs==4.0.1
basic-pitch[onnx]==0.4.0
librosa
mido
# Direct-render path (render_gb.py / gb_apu.py):
soundfile
scipy
lameenc
# demucs also needs torch; install the CPU wheels explicitly to avoid the
# multi-GB CUDA default:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
