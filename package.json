{
  "name": "project-meterfall",
  "version": "0.1.0",
  "private": true,
  "description": "Browser rhythm RPG — single-player, pixel-art, audio-clock-driven combat.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^3.80.1",
    "tone": "^15.0.4"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
